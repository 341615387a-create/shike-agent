# 验证记录

## Render公开测试版 · public-render-1 · 2026-09-24

- 新增`render.yaml`与部署说明：Render Web Service监听`0.0.0.0`和平台`PORT`，使用`/healthz`检查，DeepSeek配置来自环境变量，1 GB持久化磁盘挂载到`/var/data`。
- 公网访客通过服务端签名、HttpOnly、Secure、SameSite=Lax的匿名Cookie获得独立空间。会话列表、会话详情、个人记忆与灵感笔记均按空间隔离；跨空间读取返回404，响应中不暴露内部owner标识。
- 公网关闭模型设置与远程重启；写请求保持同源限制；同一网络默认每小时最多20次模型生成。公开首页说明对话会发送给模型服务，以及清除Cookie后无法找回记录。
- 107项程序测试通过，包括两个独立公网空间的列表/详情/画像/笔记隔离、Cookie属性、模型设置锁定、Host/Origin校验和429限流。本地旧会话继续归于默认空间。

限制：这是匿名公开测试版，不是正式账户系统。用户清除Cookie或换设备后无法恢复；IP限流可被有意绕过；SQLite加持久化磁盘只适合单实例。正式推广前仍需账户、验证码、用户额度、隐私政策、滥用监控和托管数据库。

## 消息流中的总结卡片 · summary-card-1 · 2026-09-24

- 对话总结只渲染为一张独立的「阶段总结」卡片，不再同时显示普通助手消息和下方常驻总结框。
- 最新总结默认不显示编辑器；单击卡片或桌面端右键后展开，支持修改、保存到灵感笔记与收起。较早的总结仍保持特殊样式，但只有最新一份可编辑。
- 103项程序测试和JavaScript语法检查通过。Chrome以真实本机会话只读验证：1张总结卡片、0个旧总结框、默认0个编辑器；单击与右键均能展开编辑器。未发送消息或改写用户数据。

## 致谢后不再重复 · closing-ack-1 · 2026-09-24

- 103项程序测试通过。新增覆盖「好的，谢谢」收住当前话头、「确实，有道理」仍是普通确认、长期帮助偏好不被清除、致谢时禁止追问和补建议，以及确认/致谢后高度相似回复的程序拦截。
- 使用隔离数据库和完全虚构的菜单对话调用真实DeepSeek。上一轮给出具体练习后，用户说「好的 谢谢」，模型回复「不客气，想接着弄的时候再说。」；`closing=true`，建议、计划、提问和强制帮助均关闭，原帮助偏好仍保留供以后使用。与上一回复的相似度为0.14，结果见`work/closing-ack-eval.json`，未读取或写入用户会话。
- 历史中已经显示的重复回复不做追溯删除；修复约束之后的新回复。

## 跨会话个人记忆 · personal-memory-1 · 2026-09-24

- 98项程序测试通过。覆盖真实原文来源、纯控制语排除、事实与思维方式假设边界、跨会话证据累积、可信度阈值、12条召回上限、被否定内容排除、错误提取不影响回复、纠正/恢复/永久遗忘和遗忘后不自动重建同标签。
- 使用临时数据库和真实DeepSeek完成两段独立会话。第一段自述“大二学生、做搞笑真人演绎短视频、想推进时不喜欢被劝暂停”，形成3条高可信事实并保存原文来源；第二段新会话在生成前已召回全部3条，回复明确沿用这些背景而没有要求重新自我介绍。两段共19.4秒，结果见`work/personal-memory-eval.json`，未写入用户数据库。
- Chrome在1440px与390px验证「关于你」页面、事实/假设标记、最近依据、回到来源、不准确/恢复操作和无横向溢出；没有页面脚本错误。原有聊天滚动回归同时通过。

限制：当前是单本机用户，个人记忆检索仍为词面相关性而非向量语义；显式事实的一条直接原话视为高可信，但摘要语义仍由模型生成。永久遗忘以类别和规范化标签阻止同标签重建，语义相同但标签完全不同的内容仍可能再次出现。

## 项目目标与题材不再混淆 · task-frame-1 · 2026-09-24

- 89项程序测试通过。覆盖同一任务内更换题材仍保留目标和行动帮助、排除项累积、任务替换/取消必须引用本轮原文、旧会话兼容，以及「确实」不能清空任务或被标成低能量。
- 使用临时数据库和真实DeepSeek重放三轮：请求可演示的黑客松AI项目；排除黑客松比赛流程、改从搞笑真人短视频经历取材；回复「确实」。最终task_frame保留黑客松项目目标、短视频题材、AI与可演示要求、赛事流程排除项和未解决的选题需求。
- 第二轮仍保持act帮助偏好；第三轮harness为`acknowledgement=true`、`continue_open_task=true`、`allow_question=false`。实际回复继续具体化可演示切口，没有劝停、等待或退化成拍摄技巧。三轮共35.1秒。完整隔离结果在`work/task-frame-eval.json`，未写入用户数据库。

限制：任务框架的语义更新仍先由模型判断，程序负责来源、继承和确认语边界；这降低了层级混淆，不能保证每个新任务都被概括得完美。

## 树的分层浏览与停止 / 归档 · tree-navigation-1 · 2026-09-24

- 80 项程序测试通过。新增深达 1200 层的局部记忆检索、古老相关节点召回、字符预算、完整存档不变、局部上下文更新深层节点、停止时丢弃迟到回复、立即继续输入、归档恢复不调用模型与不强制总结。
- Chrome 桌面 1440px 和手机 390px 使用浏览器内的 2952 个合成节点，检查当前深层焦点、展开/折叠、进入/返回分支、同层翻页、较早来源和足迹加载。无横向溢出、无脚本异常；保留侧栏滚动位置。聊天发送和回复的滚动回归也通过。
- 使用临时数据库调用真实 DeepSeek：1200 个存储节点，仅 16 个节点进入记忆上下文，4.5 秒完成一轮并保留旧树（完成后 1201 个节点）。合成历史仅用于连通性和结构验证，不代表自然长对话质量评估。未向用户会话插入测试消息。
- 8782 返回 tree-navigation-1；通过设置接口实际调用模型，health=connected。旧聊天记录继续使用原数据库。

限制：停止操作立即阻止提交并允许继续输入，但已经发出的外部 HTTP 请求不能保证被中途撤回。树的 DOM 和模型输入有界，整份会话的存储读取与网络传输仍随历史增长。

## 明确想开始后仍劝搁置 · support-preference-1

读取真实会话后确认：明确询问「怎么开始」虽已识别为advice，却只得到反思；后续多次明确不想搁置，又因下一条只有「对」而退回安慰。新增有原话依据的帮助偏好、同话题附和的延续、休息/换题的重置，以及对回避求助的复核。旧会话也可恢复偏好，未覆盖任何历史回复。

72项程序测试通过。真实DeepSeek隔离回放覆盖直接请求开始、明确拒绝搁置、随后只说「对」、改为休息、改聊新话题。前三种得到具体帮助，后两种不沿用行动建议许可。调试中仍遇到模型元数据错误；换题案例一轮因问题原文标记不一致失败，单独复跑成功。这些检查不能证明模型每轮都稳定或建议总是贴切。

## 混合上下文误判修复 · mixed-context-1

意图模型将「已做过某个作品，现在不知道做什么」整句误标为纯控制语，导致其中的具体信息被记忆校验拒绝。新增保守的整句控制语判断：只有完整匹配控制语的输入才从主题来源中排除；迷茫信号与是否包含信息独立。同时检查引用片段，避免只摘取「不知道」当作主题证据。

61项测试通过，包括故意注入错误的pure_control判断、混合输入保留具体信息、纯控制输入不生成主题节点。8782已加载mixed-context-1，真实失败会话通过HTTP retry成功恢复，由3条消息变为4条；只新增模型回复，原输入未重复，复古贴图信息已有原文来源节点。

## 自然聊天中的局部引导 · local-guidance-2

- 58 项单元及 HTTP 测试通过。新增相关记忆召回上限、被否定节点排除、原话校验、停问偏好延续、记忆层不能重写回复、方向字段一致性与最终复核范围测试。
- 最终版本经过真实 DeepSeek + 本地 HTTP 的连续 8 轮验证，全部完成。覆盖不知道后停问、后续仍停问、接受纠正、不同情境下的新理解、用户提出联系、切换到猫的话题、接回旧话、明确索要建议。每轮约 5–10 秒。完整结果见 `examples/local-guidance-evaluation.json`。
- 这八轮的用户输入为预先编写的测试脚本，不是用户研究，不能把脚本里的主动联想当成产品已成功激发灵感的证据。
- 调试中发现并修复了重复推断方向字段导致失败、停止提问偏好只生效一轮、第二次语义编辑过于宽泛而误拦正常比喻的问题。首轮审查完整上下文，修订后的末次复核只守提问、建议、终点与明确换题边界，不反复润色。
- Chrome 验证了桌面输入区可见、8条回复、回接旧话的来源链接、元素树、对话足迹、390px手机布局与切换，无页面脚本错误。截图 `../shike-local-guidance.png` 来自隔离的测试会话。
- 运行中的8782仍返回 `network-recovery-1`。执行同地址更新脚本时 Windows 返回 Access denied，旧进程未被结束。前端明确提示尚未更新，未改端口、未向用户会话插入测试回复。需要用户本机运行 `apply-update.cmd` 完成生效。

限制：记忆召回限同一会话，以词面重合为主；模型仍可能产生过度解读、泛泛的联想或不够准确的比喻。程序通过不等于回复质量完美，也不证明用户会产生灵感。

## 开放聊天 harness 更新

新增当轮聊天约束与独立的「先写回复、再整理树」流程；停止把主题选择视为方案授权。修订后的回复仍检查开放聊天边界，避免自行标记为share但实际仍在给做法。

- 48 项单元及 HTTP 测试通过，含连续提问上限、答不上来后停问、明确建议请求可回答、计划许可不跨轮继承、以及本机重启接口。
- 5 组真实 DeepSeek 回放最终均生成成功：AI能力自述、课堂点名尴尬、不知道、明确索要两条建议、校园猫的闲聊。记录见 `examples/open-conversation-evaluation.json`。回放中会触发修订，不代表模型一次就始终遵守约束。
- 额外通过真实模型 HTTP 连续两轮验证：普通分享没有建议许可；明确不想回答问题后禁用追问。检查同时覆盖用句号掩饰提问的常见形式，语义识别仍可能遗漏。
- 更新脚本通过 PowerShell 语法检查。Windows 拒绝开发进程终止现有的 8782 旧服务，故当前浏览器不会因源码修改自动升级；需本机运行 `apply-update.cmd`。未更换用户使用的端口。

这验证了可观察的边界，不能证明模型内部没有预期，也不能保证聊天必然产生灵感。

## 2026-09-23 连接故障修复

用户会话在旧服务 8780 上重试，0.34 秒内立即返回了笼统的「连接失败或超时」。同一输入、同一模型配置的独立进程能正常请求模型，因此不能把该失败归因为 60 秒等待超时。旧进程的确切底层异常未被原代码记录。

Windows 拒绝终止旧服务进程，修复版改在 **8782** 启动并沿用本地数据库；启动器已更新。通过新服务 HTTP API 重试用户原会话，约 5.08 秒生成回复及两个树节点，未重复追加用户输入。旧地址刷新后会自动切换到修复版，保留会话路径；切换前不会渲染可提交到旧服务的会话控件。

新增网络错误分类：权限拒绝、响应超时、DNS、TLS、连接拒绝、其他连接中断。内部诊断记录失败阶段、耗时、异常类型与系统错误码，不记录密钥。仅对暂时性错误自动重试当前模型请求一次；权限拒绝和认证错误不自动重试。

新增 5 项网络测试，累计 **36 项测试通过**。故障前的历史检查如下。

本地验证日期：2026-09-22。Python 3.14.5；Windows；Chrome；DeepSeek `deepseek-chat`。

## 确实运行过的检查

### 核心与 HTTP：31 项自动测试通过

命令：`python -m unittest discover -s tests -v`。

覆盖：提交前保存输入、创建与事件幂等、版本冲突、来源不存在或非原文引用、树更新原子性、未知答案不能成为主题证据、同义理解累计、不同理解成为兄弟分支、摘要版本保留、重复引用不增加提及次数、用户委托选路、用户偏向的焦点必须有本轮证据、点选分支不能被覆盖、暂停与生成同时发生、结束后拒绝迟到结果、失败重试不重复输入、重启恢复、编辑保存与去重、语义复核最多一次修订、密钥不返回浏览器、路径与跨站访问边界。

这些测试验证程序约束，不证明模型的所有解读都正确。

### 真实 DeepSeek：3 组场景，10 轮完成

原始输入与模型输出保存在 `examples/live-evaluation.json`，没有使用规则模板替代模型。

| 场景 | 实际检查 |
|---|---|
| 比赛前打不到车 → 我不知道 → 只能刷手机 → 请你选方向 → 改聊软件显示问题 | 不再询问「不知道和主题有什么关系」；委托选路标记 AGENT；明确转向后标记 USER，焦点转到软件节点；控制话语没有生成为主题根节点 |
| 食堂 → 喜欢热闹 → 有时感到孤独 → 今天只是声音有生活气息 | 不同感受并列保留；正面感受与孤独在共同父元素下；最后焦点跟随用户澄清，保留此前版本 |
| 有目标、方案、顾虑且明确不先谈技术的小工具想法 | 进入 EXPLORE，标记 USER，讨论参与动机 |

模型输出会变化。开发中曾发生分支引用错误、委托选路被误标为用户选路、相反理解被嵌套、复核模型过严导致无回复。已增加结构校验、独立意图识别、程序控制分叉位置，以及最多一次编辑修订；测试记录仅代表本次有限样例。

### 浏览器完整流程

使用 Playwright 驱动真实 Chrome，调用真实 DeepSeek，验证：

- 首页输入与会话创建；真实回复与元素树更新。
- 节点来源与上下文展开；点选分支后 USER 方向生效。
- 对话足迹；真实整理回复；编辑后保存到笔记。
- 暂停、刷新与继续；未发送草稿刷新后仍在。
- 390px 手机宽度切换元素树，无横向溢出。
- 设置对话框不回显密钥；没有 JavaScript 页面异常。

启动器已实际执行，固定入口为 `http://127.0.0.1:8780/`。自动打开浏览器可能受桌面权限限制，手动点击地址可访问。

## 未被证明的部分

未进行长期用户评估、长对话压力测试、不同模型兼容性矩阵或多用户测试。模型仍可能过度解读、把问题问得抽象、忽略某个潜在线索；一个合规的提问不等于有启发的提问。

结构校验能确认引文存在，不能证明从引文推导的观点成立。语义复核也由模型完成，可能漏掉臆测；不会把其结果标为「已证实」。没有联网事实检索，对外部平台机制的解释仍须自行验证。
